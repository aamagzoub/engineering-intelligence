"""
Sudanese Wist AI Laboratory GUI.

This package contains the presentation layer only.

The GUI must never contain game logic.
It only displays the current state of the engine and sends user commands
to the SimulationController.
"""